model Storage {
  id         Int      @id @default(autoincrement())
  uid        Int      @unique
  path       String
  created_at DateTime @default(now())
  updated_at DateTime @updatedAt

  @@map("__storage")
}